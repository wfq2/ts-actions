---
alwaysApply: true
---

## Development Rules

- Always make sure linting succeeds
```
npm run check
```

- Always make sure tests succeed (Do not modify tests without direct instruction to do so)
```
npm run test
```

- Add tests into the tests/ directory

- This project needs to be built using jsii, so always make sure it builds.  Additionally all interfaces need
to be compatible with jsii
```
npm run build:python
```
